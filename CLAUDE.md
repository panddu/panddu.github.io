# CLAUDE.md

## GitHub CLI (`gh`) 계정

이 레포에서 `gh` 명령어를 실행하기 전에 항상 github.com의 panddu 계정으로 전환한다:

```bash
gh auth switch --hostname github.com --user panddu
```

## ✍️ 블로그 포스트 작성 및 이미지/OG 지침

- **포스트 내 전용 이미지가 있는 경우**:
  - 해당 포스트 전용 썸네일(일러스트)을 생성했다면, 파일은 `/assets/img/posts/YYYY-MM-DD/thumbnail.jpg` 경로에 저장합니다.
  - 포스트 Front Matter에 `image: /assets/img/posts/YYYY-MM-DD/thumbnail.jpg` 형식으로 지정하여, 해당 포스트가 공유될 때 이 전용 이미지가 Open Graph(og:image)로 덮어씌워지도록 합니다.
- **포스트 내 전용 이미지가 없는 경우**:
  - Front Matter에 `image:` 필드를 명시하지 않습니다. 이 경우 `_config.yml`에 정의된 전역 공통 기본 이미지인 `/assets/img/og_default.jpg`가 자동으로 적용됩니다.

- **포스트 카테고리 분류 기준**:
  - `tech`: AI·개발·기술·업계에서 배운 구체적인 기술/지식 공유 및 튜토리얼
  - `comms`: 시청자 Q&A 및 상담소 문답 모음집
  - `review`: 장비, 서비스, 도구 등의 리뷰
  - `workroom`: 직접 개발한 미니 프로젝트나 도구 소개
  - `daily`: 단순 일상 기록, 기행문(약수터 등), 제품 구매기(테슬라 등) 등 사실 나열형 일상 글
  - `essay`: 개발자 라이프스타일, 협업에 대한 단상, 삶의 태도, 생각, 가벼운 수필 등 필자의 주관적인 통찰이나 감성이 중심이 되는 글

- **발행(`_drafts/` → `_posts/`) 시 타임스탬프**:
  - Front Matter의 `date:`는 초안을 처음 작성한 시각이 아니라 실제로 `_drafts/`에서 `_posts/`로 옮겨 발행하는 시각으로 설정합니다.
  - 이 값이 "최근 작성된 글" N 배지(`_includes/new_badge.html`, 48시간 이내 판정) 기준이라, 초안 작성 시각을 그대로 쓰면 배지가 조기에 사라지거나 애초에 뜨지 않습니다.
  - 이미 발행된 글의 `date`가 의심스러우면 `git log --all --name-status`로 `_drafts/<slug>.md` → `_posts/<date>-<slug>.md` rename 커밋 시각을 찾아 그 시각으로 정정합니다.

- **`excerpt:` 는 모든 글에 필수**:
  - `jekyll-seo-tag`가 `meta description`을 여기서 읽습니다. 없으면 본문 첫 문단을 자동으로 잘라 쓰는데, 도입부가 `<div>`나 `<style>`로 시작하는 글에서는 마크업이 그대로 들어가거나 한 줄짜리 안내문이 설명으로 나갑니다.
  - 2~3문장, 글의 핵심 소재를 담아 씁니다.

- **AI 작성 안내 배너는 본문 맨 끝에 둡니다**:
  - 위치: 본문 마지막에 `---` 구분선을 두고 그 아래. **최상단(목차 앞)에 두지 않습니다.**
  - 이유: 상단에 두면 글의 첫 텍스트가 주제가 아니라 도구 이야기가 되어 주제 신호가 약해집니다. 애드센스 심사에서도 "AI 대량 생산 콘텐츠" 프레임을 자진해서 넘겨주는 셈이 됩니다.
  - 문구: **`이 글은 <모델명>와 함께 작성되었습니다`** 로 통일합니다. `~가 작성하였습니다`는 쓰지 않습니다 — 페르소나를 넣고 함께 쓴 글인데 통째로 AI가 쓴 것처럼 읽힙니다.
  - 공개 자체는 유지합니다. 위치와 문구만 위 규칙을 따릅니다.

- **`comms`(문답) 글은 서두에 편집 리드를 씁니다**:
  - 질문·답변 목록만 나열하면 집계·전사 콘텐츠로 읽힙니다. 목록 자체가 줄 수 없는 정보를 3문단(400~700자) 정도로 앞에 얹습니다.
    1. 그 회차에서 **반복된 질문 패턴** — "19건 중 고등학생이 세 명이었습니다" 처럼, 질문을 다 읽어야 알 수 있는 걸 요약
    2. **답하면서 걸렸던 지점** — 조심스러웠던 부분, 일반화할 수 없는 한계
    3. **이전 회차와 달라진 점** — 새로 등장한 주제, 질문 성격의 변화
  - 카드 제목(`<h2 class="qna-card__theme">`)은 `13편` 같은 라벨이 아니라 주제형으로 씁니다. 예: `1편 — 진로 선택과 학벌, 신입 백엔드로 들어가는 길`
  - 이 세 개(리드 · 주제형 h2 · `excerpt`)가 한 세트입니다.

- **본문에 인라인 `<style>`을 넣지 않습니다**:
  - 글 전용 스타일이 필요하면 `assets/css/<이름>.scss`로 만들고(맨 위에 빈 front matter `---\n---` 필요), Front Matter에 `extra_css: /assets/css/<이름>.css`를 지정합니다. `_includes/head.html`이 해당 글에서만 로드합니다.
  - `main.scss`에 합치지 않는 이유: 한 글에서만 쓰는 CSS를 전 페이지에 내려보내지 않기 위함입니다.
  - 선행 사례: `_posts/2026-05-31-ddugi-first-birthday.md`의 CSS 11,986자를 `assets/css/bq-report.scss`로 분리했습니다.

- **검색 색인에서만 빼야 하는 얇은 글**:
  - Front Matter에 `noindex: true` + `sitemap: false`를 넣습니다. 사이트 내 목록·내부 링크·카테고리 카운트는 그대로 유지되고 구글 검색에서만 빠집니다.
  - 단, **이미 색인된 글에는 걸지 않습니다.** 색인된 페이지가 몇 개 안 되는 상황에서 빼면 손해입니다.

## 🔍 SEO / 애드센스 관련 주의사항

- **`_includes/head.html`에 `<link rel="canonical">`이나 atom `<link rel="alternate">`을 직접 추가하지 않습니다.**
  - `{% seo %}`(jekyll-seo-tag)와 `{% feed_meta %}`가 각각 출력합니다. 직접 추가하면 페이지마다 2개씩 나가고, 구글은 `rel=canonical`이 여러 개면 **전부 무시**합니다.
- **글 목록 페이지는 전체를 노출합니다.**
  - `posts.html`과 `_layouts/series.html`은 `post_list.html`에 `batch=9999`를 넘깁니다. 기본값(10)이면 나머지가 `.post-list__item--more`로 `display:none` 되어 아카이브가 JS "더보기" 뒤에 숨습니다. 홈(`batch=6`)은 예외입니다.
- **개인정보처리방침(`privacy.md`)은 광고 고지를 유지해야 합니다.**
  - AdSense 스크립트가 `head.html`에서 나가는 동안 `privacy.md`의 `1-4. Google AdSense` 절은 지우지 않습니다. 애드센스 프로그램 정책 요구사항입니다.

